# ===== 前端生产环境变量（自动被 vite build 加载）=====
# 参考 DEPLOY.md §6.1
#
# 推荐：前端和后端同域（Nginx 反代 /api 到后端），此时留空或直接写相对路径
VITE_API_BASE=

# 如需跨域部署（前端 a.example.com，后端 api.example.com），写完整 HTTPS URL：
# VITE_API_BASE=https://api.chain.your-school.edu.cn

# 示例：校内域名同域
# VITE_API_BASE=https://chain.your-school.edu.cn
